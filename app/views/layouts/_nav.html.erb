<div class="nav">
	<div class="left_nav">
		<h1> <%= link_to( "TRS", root_path, class: "logo" )%> </h1>
	</div>

	<div class="right_nav">
		<% if current_user? %>
			<div class="nav_link">
				<%= link_to("My Building", controller: "buildings", action: "show", id: @user.building_id)%>
			</div>
			<div class="nav_link">
				<%= link_to("My Profile", controller: "users", action: "show", id: @user.id)%>
			</div>
			<div class="nav_link">
				<%= link_to("Logout", sessions_path, :method => 'delete')%>
			</div>
		<% else %>
			<div class="nav_link">
				<%= link_to("Login", login_path) %>
			</div>
		<% end %>
	</div>
</div>
<div class="nav">
	<div class="left_nav">
		<h1> TRS Tenant Portal </h1>
	</div>

	<div class="right_nav">
		<% if logged_in? %>
			<%= link_to("My Building", building_path)%>
			<%= link_to("Logout", sessions_path, :method => 'delete') %>
		<% else %>
			<%= link_to("Login", login_path) %>
		<% end %>
	</div>
</div>
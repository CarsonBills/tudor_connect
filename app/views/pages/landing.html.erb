<h1> Welcome to Tudor Connect </h1>

<%= link_to "New User", new_user_path %>

<% if logged_in? %>
	<%= link_to("Logout", sessions_path, :method => 'delete') %>
<% else %>
	<%= link_to("Login", login_path) %>
<% end %>